---
id: p4-pageplan-llm
area: worker/copy
priority: P4
status: completed
depends_on: [p4-pageplan-schema]
---

> Bitácora: 2026-06-24 — Implementado `src/pageplan.ts` (archivos nuevos; no se tocó el árbol sucio
> preexistente de worker-copy). `buildPagePlanPrompt(dossier)` (catálogo de bloques + política
> placeholder/LG-01/AC-02, Claude-only). `parsePagePlan(raw, dossier)` = trust boundary: strip code
> fences → `PagePlan.safeParse` → policy: LG-01 (results con dígito marcado VERIFICADO → reject),
> AC-02 (testimonials verificadas sin reseñas reales en dossier → reject). `generatePagePlan` usa
> `createProvider()` (sin DeepSeek explícito). Gates: build + lint + 11 tests (6 nuevos: acepta válido
> string/obj, rechaza estructura inválida, LG-01, placeholder no-numérico OK, AC-02, prompt). Sin push.

# P4.6 — Productor de PagePlan (Claude, sin DeepSeek)

> Sub-plan de [P4-composable-blocks.md](./P4-composable-blocks.md) (entregable 5 / fase 5). Leé el padre §Entregable 5 y §"Política de datos placeholder".

## Objetivo
Subagente Claude que recibe el dossier y produce un `PagePlan`: elige bloques/orden/variantes y escribe contenido, marcando placeholder. Sin DeepSeek en ningún paso.

## Contexto necesario
- `packages/worker-copy/src/prompt.ts`, `src/index.ts` (patrón de prompt actual).
- `packages/runner/src/run-from-copy.ts` (patrón "sesión Claude como provider" ya probado).
- P4.2 (`PagePlan`). Padre P4 §Entregable 5.

## Entregables
1. Prompt nuevo (en `worker-copy` o nuevo módulo `worker-plan`): dossier → PagePlan válido. Instrucciones explícitas de bloques disponibles, política placeholder (`clase=PLANTILLA`), prohibiciones LG-01/AC-01/AC-02.
2. Parser/validador de la salida contra el schema `PagePlan` (rechaza/repara salida inválida).
3. Reusar el provider Claude existente; **no** introducir DeepSeek.

## Gates
- Build + lint OK.
- `vitest run`: test con salida LLM mockeada → PagePlan válido; test de rechazo de salida inválida (placeholder sin marcar, cifra inventada).

## Criterio de parada
Dada una salida LLM mock, produce PagePlan válido y rechaza violaciones de política; gates verdes.
