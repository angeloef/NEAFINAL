---
id: p4-quality-gate
area: validation
priority: P4
status: pending
depends_on: [p4-pageplan-schema]
---

# P4.5 — Gate de calidad QL-01..04 en `@lpb/validation`

> Sub-plan de [P4-composable-blocks.md](./P4-composable-blocks.md) (entregable 4 / fase 4). Leé el padre §Entregable 4.

## Objetivo
Subir el piso: validation deja de premiar solo "no mentir" y exige calidad estructural mínima.

## Contexto necesario
- `packages/validation/src/` (assertions AC-01/AC-02/LG-01/BR-01 existentes; el harness de contraste ya existe).
- P4.2 (`PagePlan`). Padre P4 §Entregable 4.

## Entregables (assertions nuevas, bloqueantes/altas)
- `QL-01`: mínimo de bloques (≥6) y presencia de `hero` + `contact` + `footer`.
- `QL-02`: variedad de imágenes (sin URL duplicada en la página).
- `QL-03`: datos placeholder correctamente marcados (`clase=PLANTILLA`); nada placeholder presentado como verificado.
- `QL-04`: contraste AA (reusar el existente) + jerarquía (h1 único, secciones con título).
- AC-01/AC-02/LG-01/BR-01 **intactos**.

## Gates
- Build + lint OK.
- `vitest run`: un test por QL (caso que pasa + caso que falla). Tests existentes de validation verdes.

## Criterio de parada
QL-01..04 detectan correctamente casos buenos/malos, suite de validation completa verde.
