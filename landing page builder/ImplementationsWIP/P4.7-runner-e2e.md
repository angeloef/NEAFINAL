---
id: p4-runner-e2e
area: runner
priority: P4
status: pending
depends_on: [p4-block-library, p4-image-pool, p4-quality-gate, p4-pageplan-llm]
---

# P4.7 — Runner `run-from-plan.ts` + Koziarski end-to-end

> Sub-plan de [P4-composable-blocks.md](./P4-composable-blocks.md) (entregables 6-7 / fases 6-7). Cierra P4. Leé el padre §"Plan de ejecución" 6-7 y §"Criterio de parada".

## Objetivo
Conectar todo: dossier → PagePlan (Claude) → compositor de bloques → validation(+QL) → presentation, y correr Koziarski end-to-end hasta calidad referencia.

## Contexto necesario
- `packages/runner/src/run-from-copy.ts` (análogo), `run-from-dossier.ts`, `full-pipeline.ts`.
- P4.3 (compositor), P4.4 (imágenes), P4.5 (QL gates), P4.6 (productor PagePlan).
- Output existente: `output/koziarski_obera/dossier.json`.

## Entregables
1. `packages/runner/src/run-from-plan.ts`: pipeline dossier → PagePlan → assembly → validation → presentation. CLI entry análoga a las existentes.
2. Rebuild de `@lpb/worker-assembly` antes de correr (importa desde `dist/`).
3. Corrida Koziarski end-to-end → `landing.html` autocontenido.

## Gates
1. Build + lint de los paquetes tocados.
2. `vitest run` de runner/assembly verde.
3. **Docker local** sirviendo el `landing.html`; healthcheck.
4. **Chrome MCP / Playwright** sobre el Docker: screenshot, consola sin errores, light+dark si aplica. Checklist visual vs. `ref-armando-full.jpeg`/`ref-goldberg-full.jpeg` **≥ 10/12**.
5. Review subagente: frontend → react-reviewer; datos → security-reviewer (PII, sin cifras inventadas).

## Criterio de parada
Gates de contrato + QL-01..04 verdes, ≥6 bloques, imágenes variadas, checklist visual ≥10/12. Recién ahí, P4 completo.
